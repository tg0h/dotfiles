# config
alias gpc="gpgconf"
alias gpck="gpgconf --kill gpg-agent"
alias gpca="gpgconf --list-options gpg-agent"

# list
alias gpl="gpg --list-keys"
alias gpls="gpg --list-secret-keys --keyid-format=long"

# export
alias gpe="gpg --export --armor"
alias gpes="gpg --export-secret-keys --armor"
