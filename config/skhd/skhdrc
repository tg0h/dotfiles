# https://github.com/koekeishiya/skhd/issues/1
# hyper (shift + alt + ctrl + cmd)
# meh (shift + alt + ctrl)

# enter fullscreen mode for the focused container
hyper - return : yabai -m window --toggle zoom-fullscreen


# Space
hyper - c : yabai -m space --destroy
hyper - l : yabai -m space --create
# go back to previous workspace (kind of like back_and_forth in i3)
meh - escape : yabai -m space --focus recent
# movement - cycle
hyper - r : yabai -m space --focus next || yabai -m space --focus first
hyper - g : yabai -m space --focus prev || yabai -m space --focus last
# movement - focus
hyper - 0x18 : yabai -m space --focus 1 # +
hyper - 0x1D : yabai -m space --focus 2 # )
hyper - 0x1E  : yabai -m space --focus 3 # ]
hyper - 0x1C : yabai -m space --focus 4 # *
hyper - 0x12 : yabai -m space --focus 5 # !
# move focused window to previous workspace
hyper - d : yabai -m window --space recent; yabai -m space --focus recent

# Windows
# balance size of windows
hyper - b: yabai -m space --balance

# cycle
# hyper - e : yabai -m window --focus next || yabai -m window --focus first
# hyper - u : yabai -m window --focus prev || yabai -m window --focus last

# change focus
hyper - m : yabai -m window --focus west
hyper - w : yabai -m window --focus south
hyper - v : yabai -m window --focus north
hyper - z : yabai -m window --focus east

# shift window in current workspace
hyper - h : yabai -m window --swap west || $(yabai -m window --display west; yabai -m display --focus west)
hyper - t : yabai -m window --swap south || $(yabai -m window --display south; yabai -m display --focus south)
hyper - n : yabai -m window --swap north || $(yabai -m window --display north; yabai -m display --focus north)
hyper - s : yabai -m window --swap east || $(yabai -m window --display east; yabai -m display --focus east)

# set insertion point in focused container
# alt + ctrl - h : yabai -m window --insert west
# alt + ctrl - j : yabai -m window --insert south
# alt + ctrl - k : yabai -m window --insert north
# alt + ctrl - l : yabai -m window --insert east

# move focused window to next/prev workspace
alt + cmd - 1 : yabai -m window --space 1 && yabai -m space --focus 1
alt + cmd - 2 : yabai -m window --space 2 && yabai -m space --focus 2
alt + cmd - 3 : yabai -m window --space 3 && yabai -m space --focus 3
alt + cmd - 4 : yabai -m window --space 4 && yabai -m space --focus 4
alt + cmd - 5 : yabai -m window --space 5 && yabai -m space --focus 5

hyper - 1 : yabai -m window --space 1 && yabai -m space --focus 1
hyper - 2 : yabai -m window --space 2 && yabai -m space --focus 2
hyper - 3 : yabai -m window --space 3 && yabai -m space --focus 3
hyper - 4 : yabai -m window --space 4 && yabai -m space --focus 4
hyper - 5 : yabai -m window --space 5 && yabai -m space --focus 5
hyper - 6 : yabai -m window --space 5 && yabai -m space --focus 6
hyper - 7 : yabai -m window --space 5 && yabai -m space --focus 7
hyper - 8 : yabai -m window --space 5 && yabai -m space --focus 8

# # mirror
# alt + shift - y : yabai -m space --mirror y-axis
# alt + shift - x : yabai -m space --mirror x-axis

# change layout of desktop
# alt - e : yabai -m space --layout bsp
# alt - l : yabai -m space --layout float
# alt - s : yabai -m space --layout stack

# cycle through stack windows
# alt - p : yabai -m window --focus stack.next || yabai -m window --focus south
# alt - n : yabai -m window --focus stack.prev || yabai -m window --focus north

# forwards
# alt - p : yabai -m query --spaces --space \
#             | jq -re ".index" \
#             | xargs -I{} yabai -m query --windows --space {} \
#             | jq -sre "add | map(select(.minimized != 1)) | sort_by(.display, .frame.y, .frame.x, .id) | reverse | nth(index(map(select(.focused == 1))) - 1).id" \
#             | xargs -I{} yabai -m window --focus {}

# backwards
# alt - n : yabai -m query --spaces --space \
#             | jq -re ".index" \
#             | xargs -I{} yabai -m query --windows --space {} \
#             | jq -sre "add | map(select(.minimized != 1)) | sort_by(.display, .frame.y, .frame.y, .id) | nth(index(map(select(.focused == 1))) - 1).id" \
#             | xargs -I{} yabai -m window --focus {}

# close focused window
# alt - w : yabai -m window --close

# toggle window native fullscreen
# alt + shift - f : yabai -m window --toggle native-fullscreen
